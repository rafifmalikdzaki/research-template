# Generated outputs (ignored)

One directory per run. The demo creates config, predictions, splits, metrics,
environment, and a manifest. Real training should also record checkpoint hashes
and hardware. Retain checkpoints until dependent evaluations finish and an archive
has been verified. Do not commit datasets, credentials, or large model binaries.
