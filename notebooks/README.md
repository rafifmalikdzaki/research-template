# Exploration notebooks

Install with `uv sync --locked --extra notebooks`, then run
`uv run --no-sync jupyter lab`. Clear private sample output before committing.
Move reusable transforms, models, evaluation, and figure code into `src/`.
Record the config, dataset revision, and source run for every reported figure.
