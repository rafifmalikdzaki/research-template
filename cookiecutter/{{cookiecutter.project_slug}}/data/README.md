# Data

Suggested folders: `raw/`, `interim/`, `processed/`, and `splits/`. Data is ignored
by Git. Document source, schema, checksum, group key, target definition, and access
conditions in the root README or a study protocol.
