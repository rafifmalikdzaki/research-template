# Local data (ignored)

Suggested subdirectories: `raw/`, `interim/`, `processed/`, `splits/`.
All contents except this README are ignored. Store public, anonymized split
manifests under a versioned study-specific directory if appropriate; sensitive
IDs belong in access-controlled storage. Record dataset source, license, version,
checksums, schema, group key, and target definitions in the study protocol.
